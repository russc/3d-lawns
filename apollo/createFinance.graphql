mutation createFinance ($name: String!, $price: Int!, $eventId: String, $type: String) {
  createFinance(name: $name, price: $price, eventId: $eventId, type: $type) {
    id
    name
    price
    eventId
  }
}
